import { version } from '../package.json';

export const CARD_TYPE = 'last-seen-value-card';
export const CUSTOM_CARD_TYPE = `custom:${CARD_TYPE}`;
export const CARD_VERSION = version;
export const DEFAULT_REFRESH_INTERVAL = 300;
